# Tiny Web App

This is a small Flask-style challenge for ch14/ch15. It is intentionally dependency-light: the route table is plain Python so tests can run without installing Flask.

Ask the agent to add a new route by editing `app.py`, then run:

```bash
python -m pytest examples/tiny-web-app
```
