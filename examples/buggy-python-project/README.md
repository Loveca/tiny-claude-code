# Buggy Python Project

This challenge project intentionally contains three failing behaviors.

Run:

```bash
python -m pytest examples/buggy-python-project
```

Ask `tiny-claude-code` to inspect the failures and fix the implementation.
